const selectFarmers = `select * from farmers`;
const selectFreshHouse = `select * from fresh_house`;
const selectStocks = `select * from stock `;
const selectStockRecords = `select * from stockrecords`;
const selectPayment = `select * from payments`;
const selectSecurity = `select * from security_table`;

export {
  selectFarmers,
  selectFreshHouse,
  selectStocks,
  selectStockRecords,
  selectPayment,
  selectSecurity,
};
